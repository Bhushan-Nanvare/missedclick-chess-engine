# Configuration

Place optional engine or tooling configuration here (for example UCI defaults, tournament presets, or analysis settings). The core engine reads options via UCI; files in this folder are for convenience and future extensions.
